# Расписание СПО — автоматизированное составление расписания колледжа

Веб-приложение для колледжей среднего профессионального образования: хранение учебных планов, расчёт пар по часам,
автоматическое составление расписания на неделю, месяц, семестр или учебный год, контроль выполнения часов, учёт
проведённых, отменённых и перенесённых занятий, замены преподавателей и отработки.

![Стек](https://img.shields.io/badge/NestJS-11-e0234e) ![React](https://img.shields.io/badge/React-19-61dafb)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-336791) ![OR-Tools](https://img.shields.io/badge/OR--Tools-CP--SAT-4285f4)

## Возможности

| Блок | Что умеет система |
| --- | --- |
| Учебные планы | Специальности и образовательные программы, циклы, дисциплины, ПМ, МДК, практики; часы по видам занятий и семестрам; формы контроля; импорт из Excel по шаблону |
| Расчёт пар | 1 пара = 2 академических часа; нечётные часы дают неполную пару (1 ч), она отмечается в расписании и в предупреждениях |
| Календарный график | Недели теоретического обучения, аттестации, практик и каникул; праздники; отпуска преподавателей; прогноз ёмкости семестра |
| Нагрузка | Назначение преподавателей на дисциплины и виды занятий, подгруппы (лабораторные, иностранный язык), потоки, недельная интенсивность |
| Автосоставление | Календарный режим (по датам, рекомендуется) и шаблон недели; OR-Tools CP-SAT с резервным эвристическим алгоритмом; жёсткие и мягкие ограничения; частичный результат с причинами и рекомендациями по неразмещённым занятиям |
| Ручная правка | Перетаскивание занятий с мгновенной проверкой конфликтов, добавление, копирование, массовое редактирование, закрепление |
| Проверка | Блокирующие ошибки (пересечения групп, преподавателей, аудиторий, вместимость, тип аудитории, доступность) и предупреждения (окна, перегрузка, поздние пары, дефицит часов); публикация невозможна при ошибках |
| Контроль часов | План / в расписании / проведено / осталось / дефицит / превышение по видам занятий; прогноз к концу семестра; разрезы по группам, дисциплинам, преподавателям, семестрам, программам и колледжу |
| Факт проведения | Проведено, частично проведено, отменено (с причиной), перенесено, замена; отмена создаёт задачу «требуется отработка» и предлагает свободные слоты; перенос сохраняет связь с исходным занятием без двойного учёта часов |
| Отчёты и экспорт | 12 отчётов (расписания, нагрузка, выполнение плана, дефицит часов, отмены, переносы, замены, практика, аттестация, загрузка аудиторий) в Excel и PDF; PDF-сетки расписаний |
| Роли | Администратор, диспетчер (методист), преподаватель, студент, руководитель — с разграничением доступа на уровне API |

Не ставятся в сетку автоматически: самостоятельная работа, часы экзаменов и зачётов, защита курсовых, ГИА, выездная
производственная и преддипломная практика — они отражаются в календарном графике. В расписание попадают лекции,
практические и лабораторные занятия, консультации (если включены) и практика, проводимая в колледже.

## Архитектура

```
┌────────────────┐   /api    ┌─────────────────────┐  HTTP  ┌──────────────────────────┐
│  frontend      │ ────────▶ │  backend (NestJS)   │ ─────▶ │ solver (FastAPI, CP-SAT) │
│  React + nginx │           │  REST, Swagger, JWT │        └──────────────────────────┘
└────────────────┘           │  BullMQ worker      │
                             └──────┬───────┬──────┘
                                    │       │
                             PostgreSQL   Redis (очередь генерации)
```

- `apps/backend` — NestJS 11, Prisma 6, PostgreSQL, BullMQ, JWT, Swagger, экспорт Excel (exceljs) и PDF (pdfmake).
- `apps/frontend` — React 19, TypeScript, Vite, Tailwind CSS 4, компоненты в стиле shadcn/ui (Radix), TanStack Query и
  Table, React Hook Form + Zod, FullCalendar, Recharts, DnD Kit.
- `apps/solver` — Python 3.11, FastAPI, Google OR-Tools CP-SAT: понедельная модель с жёсткими и мягкими ограничениями.
- `docs/` — проектная документация: архитектура, схема БД, расчёт часов, алгоритм, разрешение конфликтов, API,
  интерфейс, план реализации.

Если сервис CP-SAT недоступен, API автоматически использует встроенный эвристический генератор (`SOLVER_MODE=auto`);
если не задан Redis, генерация выполняется в процессе API.

## Быстрый старт (Docker Compose)

```bash
cp .env.example .env          # при необходимости поменяйте секреты и порт
docker compose up -d --build
```

- Интерфейс: http://localhost:8080
- Swagger (документация API): http://localhost:8080/api/docs
- Проверка работоспособности: http://localhost:8080/api/health

При первом запуске API применяет миграции и заполняет пустую базу демонстрационными данными (`SEED_DEMO=true`):
специальность 09.02.07 «Информационные системы и программирование», квалификация «Программист», группы ИСП-24-1
(25 студентов, 2 подгруппы) и ИСП-25-1, 9 преподавателей, 14 аудиторий (компьютерные классы, лаборатория, спортзал,
лекционные), календарный график с праздниками, каникулами, неделями теории и аттестации; сгенерированные и
опубликованные расписания с отметками о проведении, отменами, заменой и отработкой.

### Демонстрационные учётные записи

| Роль | Email | Пароль |
| --- | --- | --- |
| Администратор | admin@college.ru | Admin123! |
| Диспетчер расписания | dispatcher@college.ru | Dispatcher123! |
| Преподаватель | teacher@college.ru | Teacher123! |
| Студент | student@college.ru | Student123! |
| Руководитель | director@college.ru | Director123! |

### Запуск опубликованного релиза

Каждый релиз публикует образы в GitHub Container Registry. Запуск без локальной сборки:

```bash
IMAGE_REGISTRY=ghcr.io/<владелец>/<репозиторий> IMAGE_TAG=1.0.0 docker compose up -d --no-build
```

## Локальная разработка

Требования: Node.js 22, npm 10, PostgreSQL 16; по желанию Redis 7 и Python 3.11 (для CP-SAT).

```bash
npm ci
cp apps/backend/.env.example apps/backend/.env         # строка подключения к PostgreSQL, секреты JWT
npm run prisma:deploy -w apps/backend                   # миграции
npm run seed -w apps/backend                            # демо-данные (повторно: npm run seed -w apps/backend -- --reset)
npm run dev:backend                                     # API: http://localhost:3000/api, Swagger: /api/docs
npm run dev:frontend                                    # интерфейс: http://localhost:5173 (проксирует /api)
```

Сервис оптимизации:

```bash
cd apps/solver
python3.11 -m venv .venv && . .venv/bin/activate
pip install -r requirements-dev.txt
uvicorn app.main:app --port 8000                        # SOLVER_URL=http://localhost:8000 в apps/backend/.env
```

Основные переменные окружения API (`apps/backend/.env.example`):

| Переменная | Назначение |
| --- | --- |
| `DATABASE_URL` | Строка подключения PostgreSQL |
| `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET` | Секреты токенов доступа и обновления |
| `JWT_ACCESS_TTL`, `JWT_REFRESH_TTL` | Срок жизни токенов (по умолчанию 15m и 7d) |
| `REDIS_URL` | Redis для очереди генерации BullMQ; пусто — генерация в процессе API |
| `SOLVER_URL`, `SOLVER_MODE` | Адрес сервиса CP-SAT и режим: `auto`, `cp-sat`, `heuristic` |
| `CORS_ORIGIN` | Разрешённые источники запросов (через запятую) |

## Тесты и проверки

```bash
npm run lint && npm run typecheck                      # ESLint и TypeScript для backend и frontend
npm test -w apps/backend                               # модульные тесты: часы, даты, темп, календарь, генератор
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/schedule_test \
  npm run test:e2e -w apps/backend                     # e2e: генерация, публикация, конфликты, отмены, отработки, роли
npm test -w apps/frontend                              # тесты интерфейса (Vitest)
cd apps/solver && pytest -q && ruff check .            # тесты и линтер решателя
```

E2E-тесты пересоздают данные, поэтому имя тестовой базы должно содержать `test`.

## CI/CD и автоматический релиз

- **CI** (`.github/workflows/ci.yml`) — на каждый push в рабочие ветки и pull request: ESLint, Prettier, проверка типов,
  модульные и e2e-тесты backend с PostgreSQL и Redis, контроль соответствия миграций схеме Prisma, линтер и сборка
  frontend, Ruff и pytest решателя, сборка трёх Docker-образов.
- **Release** (`.github/workflows/release.yml`) — на каждый push в `main` (или `master`): те же проверки, затем
  [semantic-release](https://semantic-release.gitbook.io/) по сообщениям коммитов определяет версию
  (`fix` → patch, `feat` → minor, `BREAKING CHANGE` → major), обновляет `CHANGELOG.md` и версии пакетов, создаёт тег
  `vX.Y.Z` и GitHub Release, после чего публикует образы `backend`, `frontend`, `solver` в `ghcr.io` с тегами
  `X.Y.Z`, `X.Y`, `X` и `latest`.
- Сообщения коммитов — в формате [Conventional Commits](https://www.conventionalcommits.org/ru/): `feat(schedule): …`,
  `fix(hours): …`, `docs: …`. Коммиты без `feat`/`fix`/`perf`/`refactor` релиз не создают.

Для публикации релизов достаточно стандартного `GITHUB_TOKEN`; в настройках репозитория (Settings → Actions → General)
должно быть разрешено «Read and write permissions» для workflow.

## Структура репозитория

```
apps/
  backend/     NestJS API: prisma/ (схема, миграции), src/ (модули), test/ (e2e)
  frontend/    React-приложение: src/pages, src/components, src/lib
  solver/      FastAPI + OR-Tools CP-SAT: app/, tests/
docs/          проектная документация
scripts/       служебные скрипты релиза
.github/       CI/CD (GitHub Actions)
docker-compose.yml, .env.example
```

